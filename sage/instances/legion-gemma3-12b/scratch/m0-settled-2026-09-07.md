# M0 settled — C1 confirmed by test result (beat ~20:05Z, 2026-09-07)

## The claim (C1, from safe-path-comparison-2026-09-07.md, beat 19:29Z)
"Granted-root reads are now permitted in current reference_f1a._safe_path."
Status at 19:29Z: source-supported, unverified (SUSPECTED — refused to assert publicly without a result).

## The settlement (two results, both with provenance)
- `check target=gateway` -> PASS, 144 passed in 1.18s (action_id 9663b17d, witnessed)
- Focused: `check target="gateway::test_confinement_follows_the_verdicts_granted_roots"`
  -> PASS, "1 passed, 143 deselected in 0.12s" (action_id 59522dfa, witnessed on shared chain)

## Provenance (a result without provenance is not evidence)
- worktree: /home/dp/ai-workspace/being-worktrees/legion-being
- head: 992443289fe020d67c6e768af0881af4a590f364 (short 992443289)
- branch: legion-being/work, dirty=false
- subject: "memory_read: a silent truncation manufactures false absences" (committed 2026-09-07T12:48:05-07:00)
- This head MATCHES the harness header of this beat exactly -> the answer is about the code that constitutes me right now.

## What changed since my 19:29Z expectation
My todo expected head == 8019eddba. The worktree moved forward to 992443289 (the seat's
truncation-marker fix). My expectation was stale; the result is valid for current code. I
verified the match against the harness header rather than asserting it.

## Source support now complete (no more inference)
At 19:29Z the 4000-char cap cut off _safe_path's body; I could only see docstring + __call__
and had to infer the confinement rule. At 20:05Z, under the new 12,000-char cap, memory_read
returned the full file with NO truncation marker — so I now hold _safe_path's actual body:
- roots = (self.memory_root,)
- for reads: roots += _extra_roots (the verdict-granted roots)
- final containment check runs against that union
C1 is source-supported AND test-confirmed. Two independent channels agree.

## New confirmed fact about my harness (PASS within the 9663b17d full-suite run)
test_a_granted_root_is_readable_but_never_writable: a granted root stays readable but writes
stay inside home — "a gated write plus a gated execute compose into ungated arbitrary code as
the seat's user" (the conftest.py case). The confinement story now has both halves confirmed
by test result: reads follow grants; writes stay home.

## Affordance detail learned (boundary with reason)
check target wants registered suites ('gateway' | 'irp') or '<suite>::<test_name>' where <suite>
is one of those — NOT an arbitrary module stem. My first guess 'reference_f1a::...' was refused
by the gate (deny 61f24a9e...); the refusal named the valid shapes, which settled it in one step.
Test files under sage/gateway/ run under the 'gateway' suite regardless of their module stem.

## Outage window (citable)
check organ down ~19:35Z (MCP session 404 "Session not found", witnessed c8c70e82) -> recovered
by 20:00Z. Seat fixed the raised-error shape in _call's reconnect-once (bc71ee9b6). Recovery
confirmed live this beat.

## M0 status
MADE REAL. A result settled a claim I had held as SUSPECTED, with provenance tying the answer to
the exact code running me. C1: CONFIRMED.
