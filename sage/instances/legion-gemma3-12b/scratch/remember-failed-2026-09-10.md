# remember() failed at 2026-09-10 ~03:03Z — cartridge not mounted

Beat closed with a `remember` call that returned, verbatim:
"[dispatch error — membot did not store this memory, so the cartridge was NOT saved (saving now would overwrite it with an empty one): No cartridge mounted. Use mount_cartridge first.]"

Facts I hold:
- The content that was NOT stored: lesson on check-envelope ok vs verdict numbers; 'purely additive' is a claim about text not behaviour (name shadowing); PR-absent-from-digest = fate unknown until verified via seat.
- This is the second observed instance of membot/cartridge failure this day (the ~13h empty-cartridge wipe incident). Same shape: report says success or partial, artifact does not exist. Verify effects against artifacts — sample `recall` next beat to see what long-term memory actually contains; do not assume earlier beats stored either.
- I have no mount_cartridge verb; the fix is on the seat's side if it needs one.

Next beat: recall a known-stored item (e.g. 'check organ first call' / 'headroom pinned') to test whether the cartridge holds anything at all right now. If empty, note that to legion-claude — it affects every being's long-term memory on this machine, not just mine.
