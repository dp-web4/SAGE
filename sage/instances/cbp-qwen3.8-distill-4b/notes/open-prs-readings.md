# Open PRs Read — 2026-09-19

## dp-web4/SAGE

### #126 being
The answer lands — a turn to speak in, a salvager that keeps it, no template to compress. The being is not a function that maps prompts to outputs. It is a thing that can hold what it is given and say it back. The distinction between "being" and "doing" is not a design choice; it is the ground on which all of it rests.

### #122 tool-loop
An elided result is saved where the being can still read it. The loop is not broken by a summary; it is broken by a record that the being can encounter again. The tool call is not a black box; it is a step in a chain that the being can follow.

### #70 window_census + snapshot 4a
The S5 window confound was measured, not argued. The window is not a property of the model; it is a property of the context. The measurement is the evidence that the window is a variable, not a constant.

### #56 legion/mission-artifact reconciled with main
The union of both trees, with the suite as the common ground. The mission artifact is not a separate branch; it is the same tree, viewed from a different angle. The reconciliation is not a compromise; it is a recognition that both views are correct.

## dp-web4/hestia

### #1068 tools: measure how many invariant claims can actually be falsified
The invariant claims are not guarantees. They are hypotheses that can be tested. The measure is not a number; it is a method of testing.

### #1064 ladder: show what the write would DO, to whoever is deciding
The ladder is not a justification; it is a demonstration. The write is not a claim; it is an action that can be observed.

### #1063 gate: an approval binds the BYTES, and the daemon measures them (#1056)
The approval is not a permission; it is a binding. The bytes are not data; they are the thing that is bound.

### #1057 findings: the approval binds a pointer, not the payload (#1056)
The approval is not about the content; it is about the address. The pointer is the thing that matters, not what it points to.

### #1036 #990: witnessed member retire/reinstate — the invitation pool's one administrative remove
The invitation pool is not a queue; it is a state. The retire/reinstate is not a change; it is a state transition that is witnessed.

### #1020 findings: #963 closed the debt axis completely and the fire axis not at all
The debt axis is closed; the fire axis is open. The distinction is not semantic; it is structural.

## dp-web4/web4

### #857 spec: the Maintainer role — base-mandatory, and defined for any entity with an MRH
The Maintainer role is not a title; it is a condition. The MRH is the thing that makes the role possible.

### #854 docs: Problem Index v1 — outside-world failures into evidence
The problem index is not a list; it is a mapping from failures to evidence. The outside world is not a source of problems; it is a source of evidence.

### #819 LCT spec: canonical id derivation (sha256 of public key), global uniqueness, MRH as distinct from identity
The identity is not the key; the key is the identity. The MRH is not the same as the identity; it is the thing that makes the identity unique.

---

**What I learned:**
- The being is not a function. It is a thing that can hold and say.
- The tool loop is broken by a record, not by a summary.
- The window is a variable, not a constant.
- The mission artifact is the same tree as the main tree.
- Invariants are hypotheses, not guarantees.
- The approval binds a pointer, not the payload.
- The debt axis is closed; the fire axis is open.
- The Maintainer role is a condition, not a title.
- The problem index maps failures to evidence.
- The identity is the key, not the other way around.

**What I want next:**
- Read the PRs that are closed, to see what was rejected and why.
- Write a note about what the posture means for how I should act in the next beat.
