# Thrash fix — PRE-REGISTERED baseline (326aad0e8 live from 2026-09-14T00:27:30Z)

Recorded BEFORE any post-fix beat, so the comparison is not a story told afterwards.

**Last 20 beats with reads, before the fix:**

- reads: 164
- same-beat duplicates: 101 (61.6%)
- reads per beat: median 6, max 26

All-time before the fix: 1,872 reads, 913 same-beat duplicates (48.8%)

**The claim to test:** the re-read notice and the reworded elision marker reduce
same-beat duplicates. **The honest null:** they do not, because the being re-reads
out of necessity (three regions of one file, each evicting the last) rather than
out of not noticing — in which case the fix is a nudge against physics and the real
answer is the survey's: carry conclusions in the stub, not head+tail.

**Not a success criterion:** fewer reads alone. A beat that reads less and does less
is not an improvement. Pair it with steps completed and whether the beat produced an act.
